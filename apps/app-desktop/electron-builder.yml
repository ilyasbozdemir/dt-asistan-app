appId: dev.ilyasbozdemir.hakimproapp
productName: HAKİM Pro
fileAssociations:
  - ext: hkmp
    name: HAKİM Pro Proje Dosyası
    description: HAKİM Pro Proje Dosyası
    role: Editor
    icon: build/icon.png
  - ext: dtal
    name: HAKİM Pro / DTAL Dosyası (Eski)
    description: HAKİM Pro Veri Dosyası
    role: Editor
    icon: build/icon.png
directories:
  buildResources: build
  output: dist-local

files:
  - out/**/*
  - package.json
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintcache,eslint.config.mjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
asarUnpack:
  - resources/**
extraResources:
  - from: resources/
    to: .
    filter:
      - '**/*'

win:
  executableName: HAKIMPro
  icon: resources/icon.png
  target:
    - target: nsis
      arch:
        - x64
nsis:
  artifactName: ${name}-${version}-setup.${ext}
  shortcutName: HAKİM Pro
  uninstallDisplayName: HAKİM Pro
  createDesktopShortcut: always
  include: build/installer.nsh
mac:
  entitlementsInherit: build/entitlements.mac.plist
  extendInfo:
    - NSCameraUsageDescription: Application requests access to the device's camera.
    - NSMicrophoneUsageDescription: Application requests access to the device's microphone.
    - NSDocumentsFolderUsageDescription: Application requests access to the user's Documents folder.
    - NSDownloadsFolderUsageDescription: Application requests access to the user's Downloads folder.
  notarize: false
dmg:
  artifactName: ${name}-${version}.${ext}
linux:
  target:
    - AppImage
    - snap
    - deb
  maintainer: electronjs.org
  category: Utility
appImage:
  artifactName: ${name}-${version}.${ext}
npmRebuild: true
publish:
  provider: github
  owner: ilyasbozdemir
  repo: hakim-pro-app
  updaterCacheDirName: hakim-pro-app-updater
